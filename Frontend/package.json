{
  "name": "nuxt-app",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate-local": "DEV=1 nuxt generate",
    "generate": "DEV= nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "deploy": "gh-pages -d dist --dotfiles"
  },
  "dependencies": {
    "bootstrap": "^5.3.0-alpha3",
    "bootstrap-vue": "^2.23.1",
    "jquery": "^3.7.0",
    "vue": "^3.3.1",
    "vue-count-to": "1.0.13",
    "vue-meta": "^2.4.0",
    "yarn": "^1.22.19"
  },
  "devDependencies": {
    "gh-pages": "^5.0.0",
    "nuxt": "^3.3.3"
  }
}
